import { Context } from 'koa';
import UserAgent from './lib/useragent';
export declare const userAgent: (ctx: Context, next: () => Promise<void>) => Promise<void>;
declare module 'koa' {
    interface Context {
        userAgent: UserAgent;
    }
}
